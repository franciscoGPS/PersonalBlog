<div id="messages">

<% flash.each do |name, msg| %>
  <div class="alert <%= bootstrap_flash_class(name) %> fade in">
    <%= msg %>
    <button class="close" data-dismiss='alert'> x</button>
  </div>

<% end %>
</div>
